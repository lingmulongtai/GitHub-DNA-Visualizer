# Backend API base URL
# Development: leave empty — Vite dev proxy handles /api/ -> http://localhost:3001
# Production Docker Compose: VITE_API_URL=http://localhost:3001
# Production with reverse proxy at same domain: leave empty
VITE_API_URL=
